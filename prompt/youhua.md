## 需要添加的功能
效果大概是实现了，这很好。但是现在还有一些细节需要优化。
1. 首页 页面，
   1. 顶部 “首页”所在的文字部分，看看能不能去掉。
   2. 列表中的内容，除了右箭头以外要分成两个并排的部分， 
      1. 时间日期 放在一列
      2. 活动数量和心情，放到另外一列，其中 心情放在活动的上方

## 已经完成了的功能
### 项目基本架构
首先，我需要编写可以用于微信小程序端的功能，具体功能如下
1. 底部的导航栏
   1. 首页
      1. 默认打开首页，页面显示任务列表，目前只做前端页面
      2. 首页上方是一个招呼语，显示“你好，用户姓名” --- 现在暂时不介入数据库和后端服务，只是展示一个固定的文案即可，后端服务稍后再实现
   2. 我的
      1. 个人中心 --- 显示用户的个人信息，包括用户名、头像等
   3. 图表
      1. 显示用户的任务完成情况，包括已完成任务数、未完成任务数等

上面是这次需要实现的页面及内容，需要根据这些内容写相应的代码。注意：
1. 首页的任务列表目前只展示固定的文案，稍后会从数据库中获取数据。
2. 个人中心的用户信息也需要从数据库中获取，目前只展示固定的文案。
3. 图表的任务完成情况也需要从数据库中获取，目前只展示固定的文案。
4. 代码修改完成之后，需要运行程序到微信端，确认小程序代码能够正常运行




